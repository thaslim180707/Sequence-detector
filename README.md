# Sequence-detector
A **Sequence Detector** is a sequential circuit that detects a specific sequence of binary inputs and produces an output when the desired sequence occurs. This project demonstrates the detection of a predefined binary sequence using flip-flops and logic gates.
